/**
 * Date the preserved archive last received a significant site-wide update.
 * Keep this stable across builds; advance it only when page content, links, or
 * structured data materially change.
 */
export const archiveLastModified = '2026-08-01';
